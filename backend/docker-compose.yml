version: '3'
services:
  postgres:
    image: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: dw3
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data