{
  "compilerOptions": {
    "target": "ES2020",                          // Define a versão ES que será emitida (ES2020 é uma escolha moderna).
    "module": "commonjs",                        // Usado para Node.js (CommonJS).
    "outDir": "./build",                         // Pasta de saída para os arquivos compilados.
    "rootDir": "./src",                          // Diretório raiz dos arquivos TypeScript.
    "strict": true,                              // Ativa todas as verificações de tipagem rigorosa.
    "moduleResolution": "node",                  // Resolve módulos do Node.js.
    "esModuleInterop": true,                     // Permite importação de módulos ES6.
    "skipLibCheck": true,                        // Ignora a verificação de tipos nas dependências.
    "forceConsistentCasingInFileNames": true,    // Garante consistência no uso de maiúsculas/minúsculas nos imports.
    "resolveJsonModule": true,                   // Permite a importação de módulos JSON.
    "noImplicitAny": true,                       // Evita o uso implícito de `any` no código.
    "strictNullChecks": true,                    // Verifica se variáveis podem ser `null` ou `undefined`.
    "noUnusedLocals": true,                      // Reporta variáveis locais não utilizadas.
    "noUnusedParameters": true,                  // Reporta parâmetros não utilizados.
    "sourceMap": true                            // Gera arquivos de sourcemap para facilitar depuração.
  },
  "include": [
    "src/**/*.ts"                                // Inclui todos os arquivos TypeScript da pasta `src`.
  ],
  "exclude": [
    "node_modules",                              // Exclui a pasta `node_modules`.
    "build"                                      // Exclui a pasta de saída `build`.
  ]
}
